# 誰是臥底 (Who is the Spy)

社交推理派對遊戲，使用 React + Supabase 建立。

## 開發

1. 安裝套件：`npm install`
2. 建立 `.env.local`：
   ```
   VITE_SUPABASE_URL=你的_supabase_url
   VITE_SUPABASE_ANON_KEY=你的_anon_key
   ```
3. 啟動：`npm run dev`
